A test for working with flask framework

Return title, description, image for a url to have a preview link

A simple project for training. Using redis for cache purpose.

![Alt text](image.png?raw=true "Title")